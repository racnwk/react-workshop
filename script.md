# 🌷 はじめてのReact 勉強会 台本 (30分)

**構成:** 説明 約20分 + ハンズオン 約10分

**ページの進み順(画面の ←→ キーと一致):**
Home → しくみ(How)→ 見る/体験(Try)→ まとめ(Fin)→ 余談・導入(Setup)

> ※ ←→キーで台本通りに進めます。Setup は最後の「余談」枠なので、時間が押したら触れずに「あとで読んでね」と案内するだけでOK。

**凡例**
- 🎤 しゃべる内容(そのまま読んでOK・ですます調)
- 🖱 画面操作
- 💡 補足・小ネタ
- ⏱ 経過時間

---

## ⏱ 0:00 〜 0:30 / Home(30秒)

🖱 Home画面(`/`)を表示。タイトル「🌷 はじめての React 🌷」と、水をあげるとチューリップが育つ小さなティーザーが見えている状態。

🎤 みなさん、こんにちは。今日は「はじめてのReact」というテーマで、30分の勉強会をします。

🎤 先週、髙垣さんから「Reactはなぜ人気なのか」という素敵な発表があったので、今日はそれを踏まえて、Reactの **「中で何が起きているのか」** と **「どうやって使うのか」** を中心に、実際にコードを触りながら見ていきます。

🎤 前半20分が説明、後半10分はみんなで手を動かすハンズオン、という構成です。

🖱 Home の小さなチューリップ(🌱)で「💧 水をあげる」を1〜2回押して、🌱 → 🌿 → 🌷 と変わる様子をチラ見せ。

🎤 ちなみにこの小さなチューリップ、これが今日いちばん大事な例題です。「ボタンを押したら絵が変わる」という、たったこれだけの動きの中で、Reactのしくみがぜんぶ詰まっています。

🎤 それでは、しくみのページに進みます。

🖱 「💭 なんで動くんだろう?」ボタン、または ← → キーの「→」で「しくみ」へ。

⏱ 0:30

---

## ⏱ 0:30 〜 15:00 / しくみ(約14分30秒)

🖱 「しくみ」ページ(`/how`)へ。タイトル「🔍 React のしくみ 🔍」

🎤 ここからが今日のメイン、Reactのしくみです。「魔法じゃない、なんで動いているの?」を一緒に追っていきます。

### 🌟 React ってそもそもなに?(1分)

🖱 ページ冒頭「React ってそもそもなに?」のカードを表示。

🎤 まず大前提から。**Reactは、JavaScriptで書かれた「ライブラリ」** です。プログラミング言語ではなく、JSの上で動く道具です。

🎤 もう少し噛み砕くと、**誰かが書いてくれた便利なJSコードの塊**で、それをダウンロードして、自分のアプリの中から関数を呼んで使う。それだけ、というイメージでOKです。

🎤 「JSを動的にする魔法」じゃなくて、「**動的な処理を楽に書ける作法**」を提供してくれる道具、と思ってください。

⏱ 1:30

---

### 🗺 5ステップの旅(全体像、1分)

🖱 「どうやって動的な処理を楽に書くを実現してるの?」のセクションへスクロール。5ステップの図を表示。

🎤 では本題。JSXで書いたコードがブラウザに表示されるまで、**5ステップ**を通ります。これがReactのすべてです。

🎤 **① 書く** — あなたがJSXを書く
🎤 **② Babelが変換** — JSXを普通のJavaScriptに翻訳
🎤 **③ JSが実行されてオブジェクトになる** — React Element というJSオブジェクトができる
🎤 **④ ReactDOMが「ブラウザの画面」を作る** — オブジェクトを見ながら実DOMを組み立てる
🎤 **⑤ ブラウザに表示される**

🎤 ポイントは、**①と②はあなたのPCで先に終わってる**ということ。ブラウザの中で走るのは③④⑤だけです。

🎤 ※この資料のサンドボックスは例外で、学習のために②もブラウザでやっています。

🎤 ではこれを1ステップずつ見ていきます。

⏱ 2:30

---

### ① JSX を書く(2分)

🖱 「① JSX を書く」のカードへ。

🎤 旅のスタートは「あなたがコードを書くところ」。Reactで画面を作るときは、**JSX という書き方**を使います。

🎤 JSX は、**JavaScriptの中にHTMLっぽくタグを直接書ける構文**のこと。たとえば普通のJSなら `document.createElement("h1")` と書いて `textContent` を入れて…と数行かかるところを、JSXなら `<h1>Hello</h1>` の1行で済む。

🎤 ポイントを2つだけ覚えてください。

🎤 1つめ、JSXは **`.jsx` という拡張子のファイル** に書きます。そして必ず **関数の `return` の中** に書きます。

🎤 2つめ、**ブラウザはJSXを直接読めません**。だから次のステップで Babel が変換してくれる、という流れになります。

🎤 ここで覚えてほしいのが「**自作のタグも作れる**」ということ。これを **コンポーネント** と呼びます。

🎤 ルールは1つだけ。**大文字始まりのタグは自分が作った関数、小文字のタグはHTML要素として扱われる**。例えば `<Tulip />` と書くと、Tulip という関数を呼んで、その戻り値が画面になる、という意味になります。

🎤 後でTryページで実際に `<Tulip />` を作って3つ並べてみるので、いまは「タグみたいに書けるけど中身はただの関数」とだけ覚えておいてください。

🎤 ちなみに、ここから少し下にスクロールすると **Props・children・Fragment・`{}` の中で JS の値を使う書き方** など、JSX まわりのよく出てくる書き方をまとめてあります。今日は中身までは触れませんが、Try のチャレンジ④で **Props を渡す** 場面が1か所だけ出てきます。気になる人はあとで読んでみてください。

🎤 ここまでで何か質問ありますか?(ひと呼吸)

⏱ 4:30

---

### ② Babel が変換する(2分)

🖱 「② Babel が変換する」のカードへ。

🎤 ①で書いたJSXは、このままだとブラウザで動きません。`<h1>` がいきなりJSの中にあると文法エラーになるからです。

🎤 そこで登場するのが **Babel(バベル)**。**「JSコードを別のJSコードに変換するツール」** で、Reactにおいては、JSXを普通のJSに翻訳してくれる「**翻訳者**」と思ってください。

🎤 いつ動くか、というと、開発中(保存のたび)や本番ビルド時に裏で自動で動いてくれます。Vite などのビルドツールが内部で勝手に呼んでくれるので、自分で意識する必要はほぼないです。

🎤 では、実際に何に変換されるか。たとえば `<h1>🌷 こんにちは</h1>` というJSXは、こうなります。

🖱 「変換後 (実際にブラウザが読むコード)」のコードブロックを指す。

🎤 `React.createElement("h1", null, "🌷 こんにちは")` — このたった1つの関数呼び出しに化けます。

🎤 つまりJSXというのは、**`React.createElement(タグ, props, 子要素)` の書きやすい書き方**にすぎないということです。

💡 createElementの3つの引数 = HTMLの「タグ名」「属性」「中身」と対応しています。`<a href="/about" className="link">About</a>` でいうと、type が `"a"`、props が `{ href, className }`、children が `"About"`。図解があるので時間あれば見せましょう。

🎤 そして、自作タグの `<Tulip />` も同じ。`React.createElement(Tulip, null)` という形に変換されて、**type に関数そのもの**が渡される、という違いだけです。

> ⏭ **スキップ** — 資料の「🌷 Tulipコンポーネントの場合の変換 ①〜⑥」は「📖 詳しく知りたい人へ」扱い。当日は飛ばします。気になる人には「資料に6ステップ図解があるので後で読んでね」とひと言だけ。

🖱 ページ下部の「JSX → JavaScript ライブ変換器」を表示。

🎤 ここに実物があります。左にJSXを書くと、右側にBabelが変換した後のJSがリアルタイムで出ます。時間があれば後で触ってみてください。Tryページのチャレンジ①でも使います。

⏱ 6:30

---

### ③ JSが実行されてオブジェクトになる(1分30秒)

🖱 「③ JSが実行されてオブジェクトになる」のカードへ。

🎤 ②で `React.createElement(...)` の形になりました。これはただのJS関数の呼び出しなので、ブラウザが実行すると、何かが返ってきます。返ってくるのが、こんなオブジェクト。

🖱 React Elementのコードブロックを指す。`{ type: "h1", props: { children: "🌷 こんにちは" } }`

🎤 `type` と `props` を持っただけの、**ただのJavaScriptオブジェクト**。これを **React Element** と呼びます。**「画面の設計図」みたいなもの**で、まだDOMではありません。

🎤 ネストしたJSXは、設計図も入れ子の **ツリー** になります。たとえば `<div>` の中に `<h1>` と `<p>` があれば、children が配列に分かれて入ってきます。

🎤 なぜわざわざ中間にオブジェクトを挟むのか? 大事な理由が3つあります。

🎤 1つめ、**比較できる**。後でState変更時に、新旧2つのオブジェクトを比較して「どこだけ違うか」を見つける材料になります。

🎤 2つめ、**速い**。JSオブジェクトの比較は、実DOMをいちいち触るより圧倒的に速い。

🎤 3つめ、**Web以外でも使える**。同じ「設計図」でモバイル(React Native)もデスクトップも作れる、というオマケが付いてきます。

⏱ 8:00

---

### ④ ReactDOM が「ブラウザの画面」を作る(2分30秒)

🖱 「④ ReactDOM が『ブラウザの画面』を作る」のカードへ。

🎤 ③で出てきたのは、まだJSの中の「設計図」でしかありません。これを **実際の画面** に変える橋渡しが **ReactDOM** です。

🎤 ちょっと脇道に逸れますが、**DOMって何?** という人もいるかもしれないので、ひと言だけ。

🖱 「そもそも DOM って何?」の折りたたみを開く。

🎤 たとえば `<h1>Hello</h1>` というHTMLがあるとき、JSで `document.querySelector("h1").textContent = "こんにちは"` と書くと、画面の文字が変わります。このときJSが触っているのが **DOM**。**ブラウザの中にある、JSが書き換えできる画面の中身データ** のことです。

🎤 HTMLは文字が並んだただのテキスト、DOMはそれをブラウザがJSから操作できる形に組み立てたもの、と覚えておいてください。

🖱 折りたたみを閉じる。

🎤 で、ReactDOMの話に戻ります。ReactDOM は、`react-dom` という別パッケージで入ってきて、**React Elementを見ながら、実DOMを組み立てる橋渡しライブラリ** です。

🎤 すべての始まりは、プロジェクトの `main.jsx` にあるこの1行。

🖱 `ReactDOM.createRoot(document.getElementById("root")).render(<App />)` の行を指す。

🎤 HTML側にもともと `<div id="root"></div>` という**空っぽの箱**があって、ReactDOMはここを「Reactの作業場所」として確保します。あとは、`<App />` を描いてね、と指示するだけ。これは最初の1回だけ呼ばれます。あとは Reactが自動で再描画してくれます。

🎤 では、ReactDOM は内部で何をしているのか。やっていることは、たった4つです。

🎤 ① `type` を見て `document.createElement(...)` でDOM要素を作る — 「魔法」じゃなくて **普通のDOM API、ブラウザに元からある命令**を呼んでいるだけ。

🎤 ② `props` を1つずつDOMに反映 — `className` なら `el.className = ...`、`onClick` なら `addEventListener("click", ...)` のように振り分け。

🎤 ③ `children` を再帰的に処理 — 文字列なら textNode を作って入れる、また React Element ならまた①〜③を繰り返す。

🎤 ④ できあがったDOMを `root` の中にぽんと挿入する。

🎤 さらに、Reactは内部で「**この React Element はこの DOM ノードに対応してる**」という**対応マップ**も覚えておきます。これが後でState変更時の「差分だけ更新」に効いてきます。

🎤 大事なのは、**裏では魔法じゃなく、ブラウザに元からあるDOM APIを呼んでるだけ**ということ。Reactの役割は、これを自分で書かなくて済むようにしてくれること、それだけです。

⏱ 10:30

---

### ⑤ ブラウザに表示される(20秒)

🖱 「⑤ ブラウザに表示される」のカードへ。

🎤 ④で実DOMが `root` の中に挿入されると、ブラウザが「DOMが変わった」と検知して再描画してくれる。これがステップ⑤、表示です。

🎤 これで初回表示の旅は完了。**JSX → 関数呼び出し → オブジェクト → DOM操作 → 表示**。これがReactの動きの全部です。

⏱ 10:50

---

### 🎮 しくみ体感ラボ(1分)

🖱 「🎮 しくみ体感ラボ — JSX → JS → オブジェクト → DOM の旅」のカードへスクロール。4段に分かれたエディタが見える。

🎤 ステップ①〜⑤を、いま一気につないで見せます。これは **4段同期エディタ** といって、左上のJSXを書き換えると、右上のBabel変換、左下のオブジェクト、右下の実画面が、**全部リアルタイムで連動**します。

🖱 左上のJSXエディタで `<h1>🌷 こんにちは</h1>` の「🌷」を別の絵文字(例えば「🌸」)に変えてみる。

🎤 右上を見てください。Babelが瞬時に `React.createElement("h1", null, "🌸 こんにちは")` に置き換わっています。左下にはオブジェクトの形、右下には本物の画面。**この4つは別物ではなく、同じものを4段階で見ているだけ**、というのが、ここで初めて1画面で実感できます。

🎤 これがいま話してきた「魔法じゃない、ただの変換チェーン」の正体です。

⏱ 11:50

---

### 🌱 State(ステート)って何?(1分)

🖱 「State(ステート)って何?」のセクションへ。

🎤 ここまでは「初回表示」の話でした。次は **画面が変わるとき** 何が起きているか、です。その鍵になるのが **State** です。

🎤 1行で言うと、**コンポーネントが持つ「変化する値」** のことです。今の水の回数、ボタンを押した回数、入力中の文字、ログイン中かどうか — 時間とともに変わる値はぜんぶ State です。

🎤 ReactではStateを `useState` という関数で作ります。

🖱 `const [water, setWater] = useState(0);` を指す。

🎤 これで3つのものが手に入ります。`water` = 今の値、`setWater` = 更新する関数、`0` = 初期値です。

🎤 使い方は2つだけ。**読みたいときは `water` と書く、変えたいときは `setWater(新しい値)` を呼ぶ**。それだけです。

🎤 ここがReactのうれしいところで、**Stateが変わると画面が自動で書き換わってくれる**。自分で `document.getElementById` して書き換えなくていい、ということです。

⏱ 12:50

---

### 🔄 State変更時に何が起きてる?(2分30秒)

🖱 「State変更時に何が起きてる?」のセクションへ。

🎤 では「💧 水をあげる」を押した瞬間、内部では何が起きているか。順に追ってみましょう。

🎤 **①** ボタンが押されて `setWater(water + 1)` が呼ばれる。実はこれ、その場では何も起きません。Reactに「次は water=1 にしてね」と **予約のメモを渡しただけ**。同じ関数内で `water` を読んでも、まだ 0 のままです。

🎤 **②** `onClick` が終わった瞬間、Reactがキューに溜まった予約を見て「画面を作り直さなきゃ」と判断し、**App関数をもう一度頭から呼ぶ**。

🎤 ここで、ふしぎポイントがあります。2回目の `useState(0)` も走るはずなのに、なぜ値が 0 にリセットされないのか?

🎤 答えは、Reactが内部に **「コンポーネント専用のロッカー」** を持っているからです。

🖱 「ふしぎポイント:Reactはどうやって値を『裏で覚えて』るの?」のロッカー図を指す。

🎤 Appコンポーネントごとに、引き出し #0、引き出し #1、...という形でロッカーが割り当てられていて、`useState` を呼ぶたびに引き出しが1つ割り当てられます。

🎤 初回は、引き出し #0 が空っぽなので、引数の `0` を入れて、それを返す。

🎤 2回目以降は、引き出しに既に値があるので、**引数の `0` は無視して、引き出しの中身を返す**。だから値が引き継がれる。

🎤 `setWater(1)` を呼ぶというのは、この引き出し #0 に「1」を書き込む行為、というわけです。

> ⏭ **スキップ** — 資料のこのあと「🔬 もう少し専門的に:React 内部で何が起きているか」(Fiber ノード/Hook 連結リスト/memoizedState の擬似コード)があります。当日は飛ばします。「ロッカー=Fiber に紐づく連結リスト」だと頭の片隅に置きたい人だけ、あとで読んでください。

🎤 ここまでで質問ありますか?(ひと呼吸)

🎤 続けます。

🎤 **③** App関数が新しい React Element を返す。Reactはこの時点で、**前と新しい、2つの設計図** を手元に持っています。

🎤 **④** 前と新を比較。これが **Virtual DOM** と呼ばれているもの。実体は **「2つのJSオブジェクトを比較しているだけ」**。なんかすごい仕組みっぽい名前ですけど、中身はわりとシンプルです。Reactは「div の中身を 🌱 → 🌿 に」みたいな**変更リスト**を作ります。

🎤 **⑤** その変更リストを元に、ReactDOMが**必要な箇所だけ実DOMを書き換える**。全部作り直さない、これがVirtual DOMの「速さの源」です。

🎤 **⑥** ブラウザに新しい状態(🌿)が表示される。

🎤 ポイントをひとつ。**Stateが変わるたびに、関数コンポーネントは頭から最後までまるごと再実行される**。これだけは覚えて帰ってください。

> ⏭ **スキップ** — 資料のこの後ろにある「🔬 再レンダリングをもっと深く ①〜④」は「📖 詳しく知りたい人へ」扱い。当日は飛ばして次へ。気になる人には「自分の時間で読んでね」とひと言。

⏱ 15:20

---

### イベントの裏側 — ちょっとだけ予習(40秒)

🎤 もうひとつだけ予習を。さっきReactDOM が `onClick` を `addEventListener` で登録する、と話しましたが、**実はReactはボタンごとに addEventListener していません**。

🎤 ブラウザの **「総合受付」** にあたる root に **監視員を1人だけ**置いて、そこで全部のクリックを受け取り、内部の **「付箋」** を見て「このボタンを押したらこの関数」と振り分けている。

🎤 これは Try ページのチャレンジ②でじっくり見るので、ここでは「ボタン100個でも監視員は1人」とだけ覚えておいてください。

🎤 さて、しくみページはここまで。次はTryで、いま話したことが実際に目の前で動くのを見ます。

🖱 PageNavの「見る」、または → キーで Try へ。

⏱ 16:00

---

## ⏱ 15:00 〜 25:00 / 見る・体験する(Try、約10分)

🖱 Try ページ(`/try`)。タイトル「🌷 しくみを体験する 🌷」

🎤 ここからは、しくみページで説明した内容が **実際にどう動くのか** を、目で見て、手で動かして体験するコーナーです。

🎤 下にサンドボックスがあります。左にコード、右にプレビュー、その下にCONSOLEパネル。コードを編集すると、右側にリアルタイムで反映されます。

🎤 初期コードは `useState(6)` から始まっているので、最初から🌷が咲いている状態です。「💧 水をあげる」を押すと水が増え、「最初から」で 0 に戻ります。

🎤 ここでみなさんにお願いがあります。**F12キーで開発者ツールを開いてください**。Elements タブと Console タブを、これから使います。

🎤 何か壊しても、エディタ右上の **↻ リセット** で元に戻せます。何度でもチャレンジしてOK。

🎤 では、4つのチャレンジを順に紹介していきます。気になったところを実際に試してみてください。

⏱ 15:30

---

### チャレンジ①「Babel変換」をみる(2分)

🖱 「チャレンジ① 『Babel変換』をみる」のカードへ。

🎤 1つめは、**Babel が裏で動いている証拠を目で見る** チャレンジです。

🎤 ここに2つのコードが並んでます。左がJSXで書いたApp、右が同じものを `React.createElement` で書いたApp。**どちらをサンドボックスに置き換えても、表示される🌷はまったく同じ** です。Babelの仕事は、上の形を下の形に翻訳すること。それだけ。

🎤 さらに、Babel変換後の中身を **CONSOLE に出して見せる** こともできます。Appをこう書き換えてみてください。

```jsx
function App() {
  const jsx = <div style={{ fontSize: 80, textAlign: "center" }}>🌷</div>;
  console.log("Babelが作ったもの:", jsx);
  return jsx;
}
```

🎤 CONSOLEを見ると、`{ type: "div", props: { style: ..., children: "🌷" } }` というオブジェクトが出ます。

🎤 自分は `<div>🌷</div>` を書いただけなのに、CONSOLEに出るのは **オブジェクト**。これが、Babel が裏で createElement に変換した証拠であり、**React Element の正体はオブジェクト** だという、文字通り目で見える瞬間です。

💡 余裕があれば「`<p>水: {water}回</p>` だと children が `["水: ", 6, "回"]` の配列になる」も紹介。

⏱ 17:30

---

### チャレンジ②「DOM変換」をみる + onClickの謎(4分)

🖱 「チャレンジ② 『DOM変換』をみる」のカードへ。

🎤 2つめは少し盛りだくさんです。まず **ReactDOM が動いている瞬間** を目で見ます。

🎤 開発者ツールの **Elements タブ** を開いて、PREVIEW の `<p>水: 6回</p>` を見つけて選択しておきます。その状態で **💧 水をあげる** を押すと…

🎤 Elements 上で、変わった部分が **一瞬ハイライト(紫で点滅)** します。これが、**ReactDOM が今この DOM を書き換えた** という証拠です。

🎤 ここで大事なのは、`<p>` 全体が消えて新しく作られるんじゃなくて、**中の「6」というテキストだけ**が書き換わっている、ということ。これがVirtual DOMの「必要な部分だけ更新」の正体です。

🎤 さて、Elements を見ていると面白いことに気付きます。コードでは `<button onClick={...}>` と書いたのに、開発者ツールでは…

🖱 `<button style="...">💧 水をあげる</button>` のコードブロックを指す。

🎤 **`onclick="..."` という属性が無い!** これ、不思議じゃないですか?

🎤 普通のJSなら、ボタン1個ずつに `addEventListener("click", fn)` でハンドラーを貼る。でもReactは1個も貼ってない。じゃあどうやってクリックを拾ってるのか?

🎤 答えは **イベント委譲(event delegation)** という仕組みです。Reactは **root に監視員を1人だけ** 置いていて、ページ中どこのクリックでもそこに集めて捌いている。

🎤 例えるなら、**ビルの全フロアに守衛を1人ずつ置くのではなく、1階の総合受付に1人だけ**置いて全部捌くイメージ。ボタンが100個でも1000個でも、ブラウザに登録されるリスナーは常に1個だけ。

🎤 でもなぜ「受付」で全部受け取れるのか? ブラウザには **バブリング(bubbling)** という仕組みがあるからです。

🖱 バブリング図を指す。`<button>` から `<div className="card">` → `<div id="root">` → `<body>` → `<html>` と上に上がっていく図。

🎤 クリックされた要素から、親 → 親 → 親へと、イベントがDOMツリーを **下から上に登っていく**。水中の泡が下から上に登っていくように見えるので「バブリング」と呼ばれます。Reactは上流の root で待ち構えていれば、ページ中のクリックを漏らさず拾える、というわけです。

🎤 でも、もう1つ謎が残ります。「**どの関数を呼べばいい?**」をどうやって特定するのか?

🎤 ここで出てくるのが **「付箋」** です。Reactは、ボタンに直接 `__reactProps$xxxxx` という名前のプロパティを **付箋のように貼って** います。中には `{ onClick: 関数, children: ... }` という、あなたが渡したハンドラーが入っている。

🎤 だから、クリックが root に届いたとき、Reactは `event.target` で「どのボタンが押された?」を知って、そのボタンに貼られた付箋を読んで、書いてあるハンドラーを呼ぶ。それだけです。

> ⏭ **スキップ** — この後の「🔬 自分の目で確かめてみる(3ステップ)」の Console 実験(`Object.keys($0).filter(...)` と `$0[Object.keys($0).find(...)]`)は「📖 詳しく知りたい人へ」扱い。当日は飛ばして次の SyntheticEvent の話へ。時間に余裕がある時は見栄え抜群なので披露してもOK(資料 Tips 参照)。

🎤 これ、自分の目で確かめられます。**(↓当日はスキップ。資料側に手順あり)**

🖱 「自分の目で確かめてみる」のブロックを指す。

🎤 開発者ツールの Elements で **💧 水をあげる** の `<button>` タグを選択。Console に切り替えて、こう打ちます。

```js
Object.keys($0).filter(k => k.startsWith("__react"))
```

🎤 `['__reactFiber$xxxxx', '__reactProps$xxxxx']` という名前が出てきます。さらに中身を見るには、

```js
$0[Object.keys($0).find(k => k.startsWith("__reactProps"))]
```

🎤 `{ onClick: ƒ, children: [...], ... }` というオブジェクトが見えるはず。この `onClick: ƒ` こそが、Reactがボタンに貼った付箋に書かれていたハンドラー本体です。

💡 もし `onClick` が見えない場合、`<button>` 自体ではなく中の `<span>` を選んでいる可能性。Elements で `<button>` タグをクリックして選び直し。

🎤 おまけで、ハンドラーの引数 `e` についても触れておきます。`(e) => ...` のあの `e` は、ブラウザがくれた生のイベントそのものではなくて、Reactが **包み直したオブジェクト**、**SyntheticEvent(合成イベント)** といいます。

🎤 何で包むかというと、ブラウザによって細かいAPIが微妙に違う、その差をReactが吸収して統一インターフェースに変換してくれているから。あなたは1種類の書き方を覚えるだけでOK、というしくみです。

> ⏭ **スキップ** — 資料側「🤔 なぜわざわざ包む?」の3つの理由(ブラウザ差吸収/委譲システム統合/onChange等の追加機能)と React 17 のプーリング撤廃の昔話は「📖 詳しく知りたい人へ」扱い。当日はこの一言で締めて、次のチャレンジ③へ。

🎤 ※ 今日のハンズオンでは `e` は登場しません。「Reactがブラウザ差をならして渡してくれる便利オブジェクトがある」とだけ覚えておけば十分です。

⏱ 21:30

---

### チャレンジ③「State変更で再描画」をみる(2分30秒)

🖱 「チャレンジ③ 『State変更で再描画』をみる」のカードへ。

🎤 3つめは、Stateの中で何が起きているかをCONSOLEで確かめる **4つの実験** です。時間の都合で全部やる必要はなくて、好きなのを試してみてください。一番面白いのは実験2と実験4です。

🎤 **実験1: 関数まるごと再実行される証拠**

🎤 `useState` の真下に `console.log("🔄 App が呼ばれた!", water)` を1行入れて、💧 を何回か押してみる。CONSOLEに「App が呼ばれた!7」「App が呼ばれた!8」と出るはず。**Stateが変わるたびに関数まるごと再実行される**が、目で見えます。

🎤 **実験2: setWater した直後の water は何?**

🎤 これが面白い。onClick をこう書き換えます。

```jsx
onClick={() => {
  console.log("クリック前:", water);
  setWater(water + 1);
  console.log("setWater 直後:", water);
}}
```

🎤 「直後だから 7 になってる」と予想しがちですが、**実際は 6 のまま**。なぜか?

🎤 さっきの「予約」の話を思い出してください。`setWater(7)` は **「次のレンダーの water を 7 にする予約」** をしただけ。**今走っているこの関数の `water` 変数は、このレンダーの間ずっと 6 のまま固定** されています。

🎤 これ、State変更時の **よくある罠** です。「setしてすぐ読む」コードは動かないので注意。

🎤 **実験3: setWater を3回呼んでも +1 だけ**

🎤 `setWater(water + 1)` を3回連続で書いても、増えるのは +1 だけ。3回とも「古い water = 6」を使って計算するので、全部 `setWater(7)` と同じになっちゃう、ということです。

🎤 **実験4: 関数を渡せば +3 にできる(関数型更新)**

🎤 解決策はこう。値じゃなくて **関数** を渡す。

```jsx
setWater(prev => prev + 1);
```

🎤 こうすると、Reactが呼び出すたびに **「最新の値」を prev に入れて** くれる。だから3回呼べばちゃんと +3 になります。これは実務でけっこう使うやつなので、頭の片隅に。

⏱ 24:00

---

### チャレンジ④(おまけ)コンポーネント分割(1分)

🖱 「チャレンジ④ コンポーネント分割」のカードへ。

🎤 最後はおまけ。**コンポーネント分割** です。

🎤 Tulip という関数を別に作って、`stage` という props を受け取り、`<Tulip stage={plant} />` を3つ並べる、というコードがあります。コピペでOK。

🎤 これだけで、3つの🌷が同時に育つ画面ができます。**`<Tulip />` を書いた回数だけ、Tulip関数が呼ばれて、結果がそれぞれ画面に並ぶ**。コンポーネントは「関数を呼ぶショートカット」、それだけ、というのが体感できます。

🎤 ここまでで質問・困りごとあれば、ぜひ声をかけてください。

⏱ 25:00

---

## ⏱ 25:00 〜 29:00 / まとめ(Fin、約4分)

🖱 Fin ページ(`/fin`)へ。タイトル「📚 今日のまなび 📚」

🎤 30分、お疲れさまでした。

🎤 今日見てきたのは大きく2つ。**しくみ** と **State** です。

🎤 **しくみ:** JSX → Babelが変換 → JSが実行されてオブジェクトになる(React Element)→ ReactDOMが実DOMを作る → ブラウザに表示。魔法じゃない、5ステップの旅でした。

🎤 **State:** コンポーネントが持つ「変化する値」。useState で作る。Stateが変わると、関数まるごと再実行されて、画面が自動更新される。裏ではコンポーネント専用のロッカーに値が保存されている。

🎤 持ち帰ってほしい4つのポイント。

🎤 1. JSX は `React.createElement()` の **書きやすい書き方**。
🎤 2. React Element は **「ただのJavaScriptオブジェクト」**。
🎤 3. **DOM操作は React が自動でやる**。あなたは State を変えるだけ。
🎤 4. **State が変わると関数がもう一度実行される**。useStateの値が引き継がれるのは、Reactが裏で覚えてくれてるから。

🎤 次に学ぶといいものとして、useEffect(副作用)、React Router(ページ遷移)、Tailwind CSS(スタイリング)、TypeScript(型)などを挙げています。公式ドキュメント react.dev は日本語版もあるので、ぜひ覗いてみてください。

🎤 質問・感想あればぜひシェアしてください。「ここがピンとこなかった」「これって何で動くの?」、何でもOKです。

(質疑応答 〜29:00)

⏱ 29:00

---

## ⏱ 29:00 〜 30:00 / 余談・導入(Setup、時間が余れば)

🖱 Setup ページ(`/setup`)へ。タイトル「🛠 React の導入 🛠」

🎤 最後にひとつだけ、余談です。「自分のPCでReactを動かしたくなったら?」というページを用意してあります。

🎤 中身は3つ。**Vite というビルドツールの話**、**「ビルド」って何の話**、**`npm run dev` で何が起きてるかの話**。今日のしくみとは別の角度から、Reactの周辺ツールを紹介しています。

🎤 30分の枠ではここまでカバーしきれないので、**興味ある人は後で読んでみてください**。ハンズオンしたコードを自分のPCで動かしたくなったときの参考にもなります。

🎤 改めて、お疲れさまでした。ご清聴ありがとうございました!

⏱ 30:00

---

## 📌 当日忘れないTips

### ナビゲーション
- **←→キー** でページ移動。Home → How → Try → Fin → Setup の順。台本もこの順で進む
- 右下の **📝 ボタン** で、画面ごとの簡易台本パネルが開く(この `script.md` のフル版と合わせて使うと安心)
- 困ったらヘッダーの「🌷 はじめての React」をクリックでHomeに戻れる

### 開始前の準備
- ブラウザのウィンドウサイズと文字サイズを確認(プロジェクター解像度に合わせて)
- **F12で開発者ツールを開く操作** を一度自分で予行演習。Elementsタブ・Consoleタブの切り替えが必要
- Tryページのサンドボックスでひととおりチャレンジ①〜④を動かしておく

### 進行のコツ
- Tryに入る前(15分時点)に **「F12で開発者ツールを開いてください」** を必ずアナウンス。ここで開いてないとチャレンジ②が成立しない
- 「ここまでで質問ありますか?」を **しくみ後半・チャレンジ②終わり・まとめ手前** の3回入れる(教材中で巻き込みポイント指定済み)
- リセットボタンの存在を **チャレンジごとに繰り返し** 伝える
- 比喩を混ぜるとき教材と用語を合わせる:**付箋・ロッカー・引き出し・監視員・総合受付・翻訳者・予約・メモ**
- 「リスナー/コールバック」と呼ばず **「ハンドラー」** で統一

### 時間調整(押した時の削りどころ)
- **しくみ④の「ReactDOMの4ステップ詳細」** は薄くしてOK(「DOM APIを呼んでるだけ」と言い切れば10秒で終わる)。🎮 しくみ体感ラボ(1分)を入れたぶん、ここで10秒削ってトータル20秒巻ければ全体が締まる
- **しくみのVirtual DOM補足・再レンダリング深掘り** は飛ばしてOK
- **Tryのチャレンジ①(Babel変換)とチャレンジ④(コンポーネント分割)** は時間次第で省略可。残すなら **チャレンジ②と③(特に②の付箋・③の実験2・実験4)** を優先
- **Setup(余談)** は時間が無ければ口頭で「あとで読んでね」と案内するだけ
- Fin の「次に学ぶといいもの」も時間次第でカット可

### 時間に余裕がある時の追加ネタ
- しくみ②のJSX → JavaScript ライブ変換器で観客に書き換えてもらう
- チャレンジ②の `$0[Object.keys($0).find(k => k.startsWith("__reactProps"))]` を実際に打ってみせる(これは見栄え抜群)
- Setup の「`npm run dev` で何が起きる?」の全体図を1枚だけ見せる
